apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-nessie-pvc
  namespace: metastore
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Se estiver na AWS (EBS), GKE ou Azure, a storageClassName default funcionará.
  # Caso contrário, defina explicitamente:
  # storageClassName: standard
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql-nessie
  namespace: metastore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-nessie
  template:
    metadata:
      labels:
        app: postgresql-nessie
    spec:
      containers:
        - name: postgres
          # Usamos a versão 12 para garantir compatibilidade nativa com Hive 3.1.3
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "nessie_db"
            - name: POSTGRES_USER
              value: "nessie"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nessie-metastore-db-secret
                  key: postgres-password
            # Garante que os dados sejam guardados no disco persistente
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-nessie-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-nessie-storage
          persistentVolumeClaim:
            claimName: postgres-nessie-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql-nessie-service
  namespace: metastore
spec:
  selector:
    app: postgresql-nessie
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP